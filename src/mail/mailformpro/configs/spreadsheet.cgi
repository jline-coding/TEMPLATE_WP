$config{'about'} = 'Google Spreadsheet Connect Module';

## シートの名前
$config{'gsc.name'} = 'mfpdata';

## ID / 共有可能なリンクのid=のあとの文字列
$config{'gsc.id'} = '***************************************';

## 現在のウェブアプリケーションのURL
$config{'gsc.action'} = 'https://script.google.com/macros/s/***************************************/exec';

1;